// setup
int kareW =30;
int kareH =60;

void setup() {
  //bu satır pencere boyutunu ayarlar
  size (200,200);
}

void draw() {
  background(#BF4949);
  // statik verilerle aritmetik olarak komut hesabı
 // rect(200/2-30/2,200/2-30/2,30,30);
 
 //dinamik olarak pozisyon ve ölçülerin hesaplanması
 //rect(width/2-kareW/2,height-kareH/2,kareW,kareH);
 
 noStroke();
 rectMode(CENTER);
  fill(#26C81E);
 rect(width/2,height/2,kareW,kareH);
 
  fill(#E52424);
 ellipse(width/2,height/2,50,50);
 }

